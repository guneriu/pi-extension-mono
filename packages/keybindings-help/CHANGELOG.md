# @guneriu/pi-keybindings-help

## 0.2.0

### Minor Changes

- Floating keybindings reference popup: press `?` on an empty editor; two-column layout; `?`/`Esc` to close.

> Entries below this line predate Changesets and were backfilled from git history.
