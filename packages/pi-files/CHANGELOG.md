# @guneriu/pi-files

## 0.2.0

### Minor Changes

- Multi-mode preview: diff-first view for modified files, with `buildUnifiedDiff` + `getGitBaseline`.
- Track bash `mv` renames and replay them during history rebuild on reload.
- Interactive gitignore-aware project tree via `/pi-files`; peek at file contents from the tree.

> Entries below this line predate Changesets and were backfilled from git history.
