# @guneriu/pi-footer

## 0.1.0

### Minor Changes

- Enhanced footer: path, branch, session name, token counts, cache %, context window bar, Copilot cost with subagent breakdown, model + thinking level, `/custom-footer` toggle.

> Git-only package (not published to npm). Entries predate Changesets.
